action (ActionConferenceCall) {
  description (make a conference call with multiple contacts)
  type (Search)
  collect {
    input (contacts) {
      type (StructContact)
      min (Required) max (Many)
    }
  }
  output (viv.core.Text)
}
