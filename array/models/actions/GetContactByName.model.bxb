action (GetContactByName) {
  description (fetch contact structure by contact name)
  type (Search)
  collect {
    input (name) {
      type (ContactName)
      min (Optional) max (Many)
    }
  }
  output (Contact)
}
