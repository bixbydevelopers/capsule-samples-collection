structure (Contact) {
  description (name + number)
  property (name) {
    type (ContactName)
    min (Required) max (One)
  }
  property (number) {
    type (ContactNumber)
    min (Required) max (One)
  }
}
