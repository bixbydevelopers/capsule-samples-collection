structure (StructContact) {
  description (name + number)
  property (name) {
    type (TypeContactName)
    min (Required) max (One)
  }
  property (number) {
    type (TypeContactNumber)
    min (Required) max (One)
  }
}
