capsule {
  id (example.commonLayouts)
  version (0.1.0)
  format (3)
  runtime-version (7)
  targets {
    target (bixby-mobile-en-US)
    target (bixby-tv-en-US)
    target (bixby-watch-en-US)
    target (bixby-fridge-en-US)
  }
  capsule-imports {
    import (viv.core) {as (core)}
    import (viv.money) {as (money) version (2.22.26)}
    import (viv.time) {as (time) version-from (money)}
    import (viv.geo) {as (geo) version-from (money)}
  }
  store-countries {
    all
  }
  store-sections{
    section (ArtAndLifestyle)
  }
}
