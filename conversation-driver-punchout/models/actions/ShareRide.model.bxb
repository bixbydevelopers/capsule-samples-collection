action (ShareRide) {
  description (Used for sharing the route URL via punchout.)
  type (Calculation)
  collect {
    input (rideShare) {
      type (RideShare)
      min (Required) max (One)
    }
  }
  output (RouteUrl) {
    evaluate {
      $expr (rideShare.routeUrl)
    }
  }
}
