hints {
  uncategorized {
    hint ("In HTTP Requests Sample Capsule, find formal shoes with an HTTP call")
    hint ("In HTTP Requests Sample Capsule, find shoes and return headers")
    hint ("In HTTP Requests Sample Capsule, find shoes with a simple HTTP call")
  }
}