capsule {
  id (example.preferenceLearning)
  version (1.0.0)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }
  capsule-imports {
    import (viv.entity) {
      as (entity)
      version (3.14.9)
    }
    import (viv.common) {
      as (common) 
      version-from (entity)
    }
    import (viv.image) {
      as (image)
      version-from (entity)
    }
  }
}
