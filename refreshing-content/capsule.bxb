capsule {
  id (example.refresh)
  version (0.1.1)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }
  runtime-version (7)
  capsule-imports {
    import (viv.geo) {as (geo) version (9.26.48)}
  }
  store-countries {
    all
  }
  store-sections {
    section (EducationAndReference)
  }
}
