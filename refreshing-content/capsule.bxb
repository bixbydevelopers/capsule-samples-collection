capsule {
  id (example.refresh)
  version (0.1.1)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }
  runtime-version (8)
  capsule-imports {
    import (viv.geo) {as (geo) version (9.27.7)}
  }
  store-countries {
    all
  }
  store-sections {
    section (EducationAndReference)
  }
}
