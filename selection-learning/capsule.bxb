capsule {
  id (example.selectionLearning)
  version (1.0.2)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }
  runtime-version (9)
  capsule-imports {
    import (viv.money) {as (money) version (2.22.56)}
    import (viv.time) {as (time) version (3.4.10)}
  }
  store-countries {
    all
  }
  store-sections {
    section (EducationAndReference)
  }
}
