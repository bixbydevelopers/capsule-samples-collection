capsule {
  id (example.selectionLearning)
  version (1.0.1)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }
  runtime-version (2)
  capsule-imports {
    import (viv.money) {as (money) version (2.22.43)}
    import (viv.time) {as (time) version (3.1.167)}
  }
  store-countries {
    all
  }
  store-sections {
    section (EducationAndReference)
  }
}
