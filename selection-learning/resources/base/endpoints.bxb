endpoints {

  action-endpoints {
    action-endpoint (FindRideShare) {
      accepted-inputs ()
      local-endpoint (FindRideShare.js)
    }
    action-endpoint (BookRideShare) {
      accepted-inputs (rideShare)
      local-endpoint (BookRideShare.js)
    }
  }
}
