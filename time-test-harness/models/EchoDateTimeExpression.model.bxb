action (EchoDateTimeExpression) {
  type (Search)
  description (__DESCRIPTION__)
  collect {
    input (dateTimeExpression) {
      type (time.DateTimeExpression)
      min (Optional) max (Many)
    }
  }
  output (MyDateTimeExpression)
}
